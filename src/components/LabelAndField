import React from 'react';

const LabelAndField = ({ label, placeholder, type = 'text' }) => {
    return (
        <div className="label-and-field">
            <label className="label">{label}</label>
            <input className="field" type={type} placeholder={placeholder} />
        </div>
    );
};

export default LabelAndField;
